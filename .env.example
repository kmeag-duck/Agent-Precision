# Copy to .env and fill in. The workflow reads ANTHROPIC_API_KEY from the
# environment; the .env file is NOT auto-loaded, so source it manually:
#   set -a; source .env; set +a
ANTHROPIC_API_KEY=your_api_key_here
